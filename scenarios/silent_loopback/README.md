### Silent loopback scenario

This scenario should work without a CAN transciever.

Using `ch32-hal` SDIPrint for debugging.

### Running

Set your chip model in `Cargo.toml` under `ch32-hal` features.

`$ cargo run --release`
